# Gitleaks allowlist — add fingerprints of known false positives here, one per line.
# Generate with:
#   gitleaks detect --report-format json --report-path findings.json
#   jq -r '.[].Fingerprint' findings.json
# Format: <fingerprint-hex-string>  (no leading #)
